================================================================================
                     FLY-IN — TODO LIST CHRONOLOGIQUE
                  Drone Routing Simulation (sujet v1.4)
================================================================================

Légende :
  [ ] = à faire        [~] = en cours        [x] = terminé
  (!) = point de vigilance sujet     (B) = bonus

--------------------------------------------------------------------------------
PHASE 0 — SETUP DU PROJET
--------------------------------------------------------------------------------

[x] 0.1  Créer l'arborescence du projet (TOUT À PLAT) :
         fly_in/
         ├── main.py          ← créé (vide)
         ├── Makefile         ← créé (vide)
         ├── README.md        ← créé (vide)
         ├── .gitignore       ← créé (vide)
         ├── requirements.txt ← créé (vide)
         ├── map_parser.py    ← créé (vide)
         ├── zone.py          ← créé (vide)
         ├── connection.py    ← créé (vide)
         ├── drone.py         ← créé (vide)
         ├── graph.py         ← créé (vide)
         ├── router.py        ← créé (vide)
         ├── simulator.py     ← créé (vide)
         ├── renderer.py      ← créé (vide)
         ├── test_.py         ← créé (vide) — à renommer test_*.py
         ├── maps.tar.gz      ← cartes officielles du sujet (à extraire)
         └── maps/            ← dossier créé (vide pour l'instant)

[x] 0.2  Remplir .gitignore : __pycache__/, .mypy_cache/, .pytest_cache/,
         venv/, *.pyc, .DS_Store, maps.tar.gz

[x] 0.3  Remplir requirements.txt : flake8, mypy, pytest, colorama

[x] 0.4  Créer un venv (.venv) et l'activer + make install
         → python3 -m venv .venv && source .venv/bin/activate && make install

[x] 0.5  Rédiger le Makefile avec les règles imposées :
         - install, run, debug, clean, lint, lint-strict ✓

[x] 0.6  Extraire maps.tar.gz dans maps/ :
         → tar -xzf maps.tar.gz

[x] 0.7  Vérifier que `make lint` tourne (sur fichiers vides)

  --- Tests de validation PHASE 0 ---
[x] T0.1  `make lint` → 0 erreurs flake8 et mypy sur tous les fichiers
[x] T0.2  `make install` → flake8, mypy, pytest, colorama bien installés
[x] T0.3  `ls maps/` → les 10 cartes officielles présentes
          (easy x3, medium x3, hard x3, challenger x1)

--------------------------------------------------------------------------------
PHASE 1 — MODÈLES DE DONNÉES (fichiers à la racine)
--------------------------------------------------------------------------------

[x] 1.1  zone.py :
         - Enum ZoneType { NORMAL, BLOCKED, RESTRICTED, PRIORITY }
         - Classe Zone (dataclass) : name, x, y, zone_type, color,
           max_drones, current_drones (set/list)
         - Méthodes : is_full(), can_accept(), add_drone(), remove_drone(),
           movement_cost() (1 pour normal/priority, 2 pour restricted)
         (!) start et end ont capacité illimitée — flag is_start/is_end

[x] 1.2  connection.py :
         - Classe Connection : zone_a, zone_b, max_link_capacity,
           drones_in_transit
         - Méthodes : connects(zone_name), other_end(zone_name),
           has_capacity(), occupy(), release()
         - __eq__ et __hash__ symétriques (a-b == b-a)
         (!) Sujet VII.4 : duplicates a-b et b-a interdits

[x] 1.3  drone.py :
         - Enum DroneStatus { AT_START, MOVING, IN_TRANSIT, ARRIVED }
         - Classe Drone : id, current_zone, planned_path (list[Zone]),
           path_index, status, in_transit_connection, turns_remaining_transit
         - Méthodes : next_zone(), is_arrived(), start_transit(), tick_transit()

[x] 1.4  graph.py :
         - Classe Graph : zones dict[str, Zone], connections list[Connection],
           start_hub, end_hub
         - Méthodes : add_zone(), add_connection(), get_zone(),
           get_neighbors(zone), get_connection(z1, z2), validate()
         - Détection de doublons, validation cohérence (start/end uniques,
           connexions pointent vers zones existantes)
         (!) Aucune librairie graphes — tout from scratch

  --- Tests de validation PHASE 1 ---
  zone.py
[x] T1.1  ZoneType : 4 valeurs (NORMAL, BLOCKED, RESTRICTED, PRIORITY)
[x] T1.2  movement_cost() : RESTRICTED → 2, tous les autres → 1
[x] T1.3  is_full() : jamais True si is_start ou is_end
[x] T1.4  is_full() : True quand occupancy >= max_drones
[x] T1.5  add_drone() : lève ValueError si zone blocked ou pleine
[x] T1.6  remove_drone() : lève KeyError si drone_id absent
  connection.py
[x] T1.7  connects() : True pour zone_a et zone_b, False pour autre nom
[x] T1.8  other_end() : retourne l'opposé ; lève ValueError si inconnu
[x] T1.9  occupy() + has_capacity() + release() : cycle complet
[x] T1.10 __eq__ et __hash__ : a-b == b-a ; set déduplique a-b/b-a
  drone.py
[x] T1.11 next_zone() : None si path vide ou si déjà à la dernière zone
[x] T1.12 start_transit() + tick_transit() : 1 tick → retourne True
[x] T1.13 is_arrived() : False par défaut, True si status ARRIVED
  graph.py
[x] T1.14 add_zone() + get_zone() : accès par nom, KeyError si inconnu
[x] T1.15 add_connection() : rejette doublons a-b / b-a (ValueError)
[x] T1.16 get_neighbors() : liste les zones voisines d'une zone donnée
[x] T1.17 get_connection(z1, z2) : retrouve la connexion dans les 2 sens
[x] T1.18 validate() : lève erreur si start_hub ou end_hub manquant
[x] T1.19 `make lint` → 0 erreurs sur zone.py, connection.py,
          drone.py, graph.py

--------------------------------------------------------------------------------
PHASE 2 — PARSER (map_parser.py)
--------------------------------------------------------------------------------

[x] 2.1  Classe MapParser avec méthode parse(filepath) -> Graph
         (Sessions A, B et C faites — 2.7 et 2.8 terminés)

[x] 2.2  Lire le fichier avec `with open(...)` (context manager OBLIGATOIRE)

[x] 2.3  Ignorer les commentaires (#) et les lignes vides

[x] 2.4  Parser `nb_drones: <int>` (première ligne non-commentée)
         (!) Sujet VII.4 : doit être un entier positif

[x] 2.5  Parser les zones (Session B — avec str.split, pas regex) :
         - start_hub: <name> <x> <y> [metadata]
         - end_hub:   <name> <x> <y> [metadata]
         - hub:       <name> <x> <y> [metadata]
         - Metadata : zone=..., color=..., max_drones=...
         - Tags dans [] dans n'importe quel ordre
         (!) Noms : pas de tiret, pas d'espace
         (!) Coordonnées : entiers
         (!) Types valides : normal, blocked, restricted, priority

[x] 2.6  Parser les connexions : `connection: <a>-<b> [max_link_capacity=N]`
         (!) Les deux zones doivent déjà être déclarées
         (!) a-b et b-a sont des doublons à rejeter

[x] 2.7  Validation finale :
         - Exactement 1 start_hub et 1 end_hub
         - Noms de zones uniques
         - max_drones et max_link_capacity sont des entiers positifs
         (déjà couverte : Graph.add_zone rejette les doublons,
          Graph.validate() exige start/end, _resolve_*() valident
          les entiers positifs)

[x] 2.8  Lever des exceptions claires avec n° de ligne et cause
         (!) Sujet VII.4 : "stop the program and return a clear error message
             indicating the line and cause"
         (try/except ValueError → MapParseError(line_no) dans
          _parse_zone, _parse_connection et parse)

[x] 2.9  Tests unitaires dans test_.py (ou renommer test_parser.py) :
         - Cartes valides minimales
         - Erreurs : double start, type invalide, connexion dupliquée,
           connexion vers zone inexistante, capacité négative, tiret dans nom
         (test_session_b.py : 20 tests zones + nb_drones
          test_session_c.py : 14 tests connexions = 34 tests OK)

  --- Tests de validation PHASE 2 ---
[x] T2.1  Parser une carte minimale : 1 start, 1 end, 1 connexion,
          nb_drones correct → Graph valide retourné sans exception
[x] T2.2  Metadata optionnelle : zone=restricted, color=red,
          max_drones=3 → attributs bien chargés sur la Zone
[x] T2.3  Commentaires (#) et lignes vides silencieusement ignorés
[x] T2.4  Erreur ligne+cause : double start_hub détecté
[x] T2.5  Erreur ligne+cause : type de zone invalide (ex: zone=flying)
[x] T2.6  Erreur ligne+cause : connexion dupliquée (a-b puis b-a)
[x] T2.7  Erreur ligne+cause : connexion vers zone inexistante
[x] T2.8  Erreur ligne+cause : capacité négative ou non-entière
[x] T2.9  Erreur ligne+cause : tiret dans le nom d'une zone
[x] T2.10 `make lint` → 0 erreurs sur map_parser.py

--------------------------------------------------------------------------------
PHASE 3 — CARTES DE TEST (maps/)
--------------------------------------------------------------------------------

[x] 3.1  Extraire maps.tar.gz dans maps/ :
         tar -xzf maps.tar.gz
         Résultat attendu :
         maps/easy/01_linear_path.txt       (2 drones)
         maps/easy/02_simple_fork.txt       (4 drones)
         maps/easy/03_basic_capacity.txt    (4 drones)
         maps/medium/01_dead_end_trap.txt   (5 drones)
         maps/medium/02_circular_loop.txt   (6 drones)
         maps/medium/03_priority_puzzle.txt (5 drones)
         maps/hard/01_maze_nightmare.txt    (8 drones)
         maps/hard/02_capacity_hell.txt     (12 drones)
         maps/hard/03_ultimate_challenge.txt (15 drones)
         maps/challenger/01_the_impossible_dream.txt (25 drones)

[x] 3.2  Lire et comprendre chaque carte officielle avant de coder
         (valider manuellement que le format correspond bien au sujet)

[x] 3.3  Créer des cartes d'erreur pour tester le parser :
         maps/invalid/double_start.txt
         maps/invalid/bad_zone_type.txt
         maps/invalid/duplicate_connection.txt
         maps/invalid/unknown_zone_in_connection.txt
         maps/invalid/negative_capacity.txt
         maps/invalid/dash_in_name.txt

[x] 3.4  Créer des cartes edge case :
         maps/edge/one_drone.txt      — 1 seul drone
         maps/edge/direct_path.txt    — start connecté directement à end
         maps/edge/blocked_only.txt   — seul chemin bloque (no solution)

  --- Tests de validation PHASE 3 ---
[x] T3.1  Charger les 10 cartes officielles sans exception
[x] T3.2  nb_drones correct pour chaque carte officielle
          (vérifier les valeurs attendues depuis le sujet)
[x] T3.3  Chaque carte de maps/invalid/ lève bien une exception
          avec numéro de ligne et description de la cause
[~] T3.4  Cartes edge case : 1 drone passe, chemin direct passe,
          blocked_only → comportement attendu défini et testé
          (parsing OK ; no-solution behavior à tester en Phase 4)

--------------------------------------------------------------------------------
PHASE 4 — PATHFINDING (router.py)
--------------------------------------------------------------------------------

[x] 4.1  Implémenter Dijkstra from scratch (avec heapq autorisé) :
         - Coûts : normal=1, priority=1 (avec léger bonus si tie-breaker),
           restricted=2, blocked=inf
         - Retourne (cost, path) ou None si pas de chemin
         (Dijkstra + tie-breaker PRIORITY via tuple (cost, penalty, name)
          + paramètres blocked_zones/blocked_edges pour les chemins multiples)

[x] 4.2  Implémenter une recherche de chemins multiples disjoints :
         - Greedy capacités résiduelles : Dijkstra répété en décrémentant
           zone_load (max_drones) et edge_load (max_link_capacity)
         - Router.find_disjoint_paths(max_paths) → list[Path]
         - Chaque lane = 1 unité de flot (doublons = capacité parallèle)
         (!) Sujet VII.1 : "Distribution of drones across multiple paths"

[x] 4.3  Classe PathPlanner :
         - find_all_useful_paths(nb_drones) -> list[Path]
         - assign_drones_to_paths(nb_drones, paths) -> list[Path]
         - plan(nb_drones) -> list[Path]  ← point d'entrée Simulator
         - Stratégie gloutonne lem-in : min(counts[i] + path.cost)
         - NoSolutionError si aucune route disponible

[x] 4.4  Calculer le débit (throughput) d'un chemin = min des capacités
         des zones et connexions le long du chemin
         (Router.path_throughput() — skips start/end, prend min global)

[x] 4.5  Optimisation : prioriser zones priority en cas d'égalité de coût
         (mécanisme penalty dans tuple heap : 0 si PRIORITY, +1 sinon)

[x] 4.6  Tests pathfinding : chemins triviaux, sans solution, multi-chemins,
         priorité aux priority, distribution PathPlanner
         (9 tests dans test_router.py — tous verts)

  --- Tests de validation PHASE 4 ---
[x] T4.1  Dijkstra : chemin trivial start → end direct (1 connexion)
[x] T4.2  Dijkstra : retourne None si tous les chemins sont bloqués
[x] T4.3  Dijkstra : zone RESTRICTED comptée à 2 dans le coût total
[x] T4.4  Dijkstra : zone PRIORITY préférée en cas d'égalité de coût
[x] T4.5  Chemins multiples : ≥ 2 chemins disjoints trouvés sur une
          carte avec fork (easy/02_simple_fork.txt) — path_a ET path_b
[x] T4.6  Débit d'un chemin = min(capacités zones + connexions du path)
[x] T4.7  `make lint` → 0 erreurs sur router.py

--------------------------------------------------------------------------------
PHASE 5 — MOTEUR DE SIMULATION (simulator.py)
--------------------------------------------------------------------------------

[x] 5.1  Classe Simulator :
         - __init__(graph, nb_drones)  [planner/drones/history construits ici]
         - run() -> int (nombre de tours)
         - history: list[list[Movement]]
         + dataclass Movement(drone_id, destination, is_transit) + __str__
         + snapshots: list[dict[int, str]] (frames pour Phase 6 graphique)

[x] 5.2  Initialisation : tous les drones placés au start_hub
         (!) start a capacité illimitée — pas de vérification de max_drones

[x] 5.3  Boucle principale (while not tous_arrivés) :
         a) Calculer les intentions de mouvement pour chaque drone
         b) Résoudre les conflits (capacités zones + connexions)
         c) Appliquer les mouvements valides en SIMULTANÉ
         d) Décrémenter les compteurs transit (restricted)
         e) Logger les mouvements du tour
         f) Incrémenter turn_count

[x] 5.4  Résolution des mouvements simultanés :
         - Algorithme : un drone sortant libère sa place AVANT que d'autres
           tentent d'y rentrer dans le même tour
         - Implé : tri des drones par path_index décroissant + application
           immédiate sur les modèles (la place libérée est vue par can_accept)
         (!) Sujet VII.3 : "Drones moving out of a zone free up capacity
             for that same turn"

[x] 5.5  Gérer le transit vers restricted (2 tours) :
         - Tour T : drone entre dans la connexion (output: D1-<connection>)
         - Tour T+1 : drone arrive dans la zone (output: D1-<zone>)
         - Implé : compteur incoming[zone] pour réserver la place à T+1
         (!) Sujet VII.3 : "It can't wait extra turns on the connection"
         (!) Doit donc vérifier à l'avance que la zone destination aura
             de la place au tour T+1 avant de s'engager

[x] 5.6  Stratégie d'attente : un drone bloqué reste sur place (omis du log)

[x] 5.7  Détection de boucle infinie (limite max_turns=1000 -> RuntimeError)

[~] 5.8  Tests simulation :  (validé MANUELLEMENT le 2026-05-26 via scripts,
         test_simulator.py pytest formel RESTE À ÉCRIRE)
         - Tous arrivent
         - Respect capacités
         - Restricted prend bien 2 tours
         - Pas de collision

  --- Tests de validation PHASE 5 ---
  (NB : validés manuellement sur easy/* + medium/* ; à formaliser en pytest)
[x] T5.1  1 drone, chemin direct : arrive en 1 tour, status ARRIVED
          -> edge/one_drone : 1 tour OK
[~] T5.2  Capacités respectées : 2 drones jamais dans la même zone
          (max_drones=1) au même tour  [observé OK, pas de test pytest dédié]
[x] T5.3  Transit restricted : log sur connexion au tour T,
          log dans la zone au tour T+1 — exactement 2 tours
          -> medium/02 (exit_point) et medium/03 (slow_path1) OK
[x] T5.4  Libération simultanée : drone sortant libère sa place pour
          un entrant dans le même tour (règle VII.3)  -> easy/linear OK
[x] T5.5  Simulation complète : tous les drones finissent ARRIVED
          -> easy/* (4t) + medium/* (8/15/7t) sous les benchmarks
[ ] T5.6  Détection boucle : lève une erreur si max_turns dépassé
          [implémenté mais PAS encore testé]
[x] T5.7  `make lint` → 0 erreurs sur simulator.py

--------------------------------------------------------------------------------
PHASE 6 — AFFICHAGE (renderer.py)
--------------------------------------------------------------------------------

[x] 6.1  Classe Renderer (ABC) + TerminalRenderer :
         - Renderer(ABC) : interface commune, méthode abstraite render()
         - TerminalRenderer(Renderer) : render() → format sujet VII.5
         (!) Sujet VII.5 : drones immobiles omis, drones arrivés disparaissent

[x] 6.2  Output coloré terminal (colorama) :
         - Couleur stable par drone (cycle Fore.GREEN/CYAN/YELLOW/...)
         - En-tête "Fly-in - N drones, T turns"
         (!) Sujet VII.1 : représentation visuelle OBLIGATOIRE

[~] 6.3  Visualisation graphique matplotlib (graphical_renderer.py) :
         - GraphicalRenderer(Renderer) avec FuncAnimation
         - Graphe animé : nœuds coords x,y, drones frame par frame
         - Snapshots (Simulator.snapshots) comme source de données
         [EN COURS — à implémenter]

[ ] 6.4  Mode summary final : nombre de tours, drones livrés par tour,
         coût total

  --- Tests de validation PHASE 6 ---
[x] T6.1  Format `D<ID>-<zone>` séparés par des espaces — confirmé
          (ex : "D1-waypoint1", "D1-waypoint2 D2-waypoint1")
[x] T6.2  Drone immobile : absent de la ligne du tour (omis du log)
          (garanti par Simulator._step qui ne log que les Movement produits)
[x] T6.3  Drone en transit : logge le nom de la connexion, pas la zone
          (Movement.is_transit + label "zone_a-zone_b" dans _try_enter_restricted)
[x] T6.4  Drone arrivé : absent de tous les tours suivants
          (DroneStatus.ARRIVED → exclu par _active_drones_ordered)
[x] T6.5  `make lint` → 0 erreurs sur renderer.py

================================================================================
  >>> MEMO PERSONNEL — QUÊTE SECONDAIRE : VISUALISATION 3D <<<
================================================================================
  Idée à réaliser APRÈS avoir terminé le rendu terminal obligatoire (6.1-6.2)
  et SI il reste du temps avant la deadline.

  Concept : visualisation 3D espace-temps avec matplotlib (mpl_toolkits.mplot3d)
    - Axes X, Y : coordonnées des zones depuis la carte
    - Axe Z     : numéro du tour de simulation
    - Résultat  : les trajectoires des drones apparaissent comme des lignes 3D
                  qui montent dans le temps — on voit visuellement les chemins
                  empruntés, les croisements, les attentes, les goulots.

  Pourquoi c'est intéressant :
    - matplotlib est déjà dans requirements.txt (pas de nouvelle dépendance)
    - Permet de comprendre d'un coup d'œil la stratégie de routing
    - Excellent visuel à montrer en peer review
    - Aucun conflit avec le sujet (pas de librairie de graphes utilisée)

  À noter :
    - PAS de points bonus officiels (le bonus chap. IX ne couvre que la perf
      et la Challenger map). C'est une quête secondaire personnelle.
    - Ne pas commencer avant que tout le mandatory (phases 1-7) soit validé.

  Pistes d'implémentation :
    - Classe Renderer3D séparée du renderer terminal
    - Méthode render_simulation(history) qui dessine TOUTES les trajectoires
    - Couleur par drone, marqueurs aux zones, légende
    - Option : animation step-by-step avec matplotlib.animation
================================================================================

--------------------------------------------------------------------------------
PHASE 7 — POINT D'ENTRÉE (main.py)
--------------------------------------------------------------------------------

[x] 7.1  Parse arguments CLI (sys.argv) : fichier carte (sys.argv[1])

[x] 7.2  Try/except global : FileNotFoundError, MapParseError,
         NoSolutionError, RuntimeError → message propre + exit code 1
         (!) Sujet III.1 : crash = projet non-fonctionnel

[x] 7.3  Orchestration : MapParser → Simulator.run() → TerminalRenderer
         (classe Application + fonction main() → séparation OO / argv)

[x] 7.4  Code de sortie : 0 si succès, 1 si erreur, 2 si mauvais usage

  --- Tests de validation PHASE 7 ---
[x] T7.1  exit code 0 sur carte valide — confirmé (easy/01, code 0)
[x] T7.2  exit code 1 + message sur carte invalide — implémenté (_fail)
[x] T7.3  message d'usage si aucun argument — implémenté (len(argv) != 2)
[x] T7.4  Orchestration complète sur easy/01 : "Fly-in - 2 drones, 4 turns"
          + 4 lignes D<ID>-<zone> — confirmé visuellement
[x] T7.5  `make lint` → 0 erreurs sur main.py

--------------------------------------------------------------------------------
PHASE 8 — TESTS & VALIDATION
--------------------------------------------------------------------------------

[ ] 8.1  Couvrir parser, models, pathfinding, simulator avec pytest

[ ] 8.2  Vérifier flake8 (ligne max 79, PEP8)

[ ] 8.3  Vérifier mypy avec les flags du Makefile

[ ] 8.4  Tester sur TOUTES les cartes fournies dans le sujet

[ ] 8.5  Mesurer le nombre de tours vs targets (VII.7) :
         - Easy ≤ 6-8 tours
         - Medium ≤ 12-15 tours
         - Hard ≤ 30-45 tours

[ ] 8.6  (!) Tester edge cases : 1 drone, 1000 drones, graphe non connexe,
         carte avec uniquement start et end, zone blocked sur seul chemin

--------------------------------------------------------------------------------
PHASE 9 — DOCUMENTATION (README.md)
--------------------------------------------------------------------------------

[ ] 9.1  README.md en ANGLAIS, première ligne en *italique* :
         "*This project has been created as part of the 42 curriculum by
         <login42>.*"

[ ] 9.2  Section Description (but du projet, vue d'ensemble)

[ ] 9.3  Section Instructions (make install, make run MAP=file.txt, etc.)

[ ] 9.4  Section Resources (Dijkstra, Suurballe/Yen's algo, articles)
         + mention claire de l'usage d'IA et pour quelles tâches

[ ] 9.5  Section Algorithm (choix d'algo, stratégie de distribution,
         gestion des conflits) — REQUIS par le sujet (chap. VIII)

[ ] 9.6  Section Visual Representation (description de l'affichage)

--------------------------------------------------------------------------------
PHASE 10 — BONUS (B)
--------------------------------------------------------------------------------

[ ] 10.1 (B) Atteindre TOUS les targets de performance "perfectly"
[ ] 10.2 (B) Résoudre Challenger map "The Impossible Dream" en < 45 tours
[ ] 10.3 (B) Interface graphique avancée (animation, replay, contrôles)
[ ] 10.4 (B) Affichage stats secondaires : drones par tour, coût total

--------------------------------------------------------------------------------
CHECKLIST FINALE AVANT SOUMISSION
--------------------------------------------------------------------------------

[ ] make lint  → 0 erreurs flake8 ET mypy
[ ] make run MAP=... → ne crash sur AUCUNE carte (valide ou invalide)
[ ] README.md respecte la première ligne italique avec login 42
[ ] Tous les fichiers sont sous le repo Git (chap. X)
[ ] Aucune librairie de graphes utilisée (grep networkx, graphlib)
[ ] Tous les fichiers ont des docstrings et type hints
[ ] Aucune fonction > 25 lignes sans justification claire
[ ] Capable d'expliquer chaque ligne de code à l'oral (peer review)

================================================================================
                            FIN DU TODO — Good luck!
================================================================================
